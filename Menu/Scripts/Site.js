$(document).ready()
{
    $('a.glyphicon-plus').on('click', function () {

        /*
        var html = $('html');
        //Retrieve positions
        var nextPos = html.attr('data-position')
        var currentPos = html.css("position");
        //Save scroll value
        if (currentPos != "fixed") {
            $('html').attr('data-scroll', $('html').scrollTop());
        }
        //Update position
        html.attr('data-position', currentPos);
        html.css('position', nextPos);
        //Restore scroll value
        if (currentPos == "fixed") {
            $('html').scrollTop($('html').attr('data-scroll'));
            $('html').removeAttr('data-scroll');
        }*/
    })
}